# P1 — High

_No open P1 items._

- **WID-P1-1** — dead raw-WebSocket voice stack removed (`useLiveCall`, `LiveCallInputArea`, `CallButton`, `LiveCallOverlay`, all `LiveCall*` types + tests + barrel exports). The mounted widget uses the socket.io `VoiceCallOverlay` path. No public API change. — ✅ RESOLVED v1.7.3.
